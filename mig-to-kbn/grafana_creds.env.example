# Copy to grafana_creds.env and source it before Grafana API mode commands.
# These three are used for dashboard extraction.
GRAFANA_URL=http://localhost:23000
GRAFANA_USER=admin
GRAFANA_PASS=admin

# Optional: used by alerting API calls in environments that prefer bearer auth.
GRAFANA_TOKEN=
