source: count(up == 1)
feasibility: feasible
warning: count() over a comparison is approximated as document COUNT(*); multi-sample series may be over-counted
---
TS metrics-*
| WHERE @timestamp >= ?_tstart AND @timestamp <= ?_tend
| WHERE up == 1
| STATS up_count = COUNT(up)
