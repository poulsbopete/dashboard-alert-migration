source: node_memory_SwapFree_bytes / (node_memory_SwapTotal_bytes > bool 0)
feasibility: feasible
warning: Approximated PromQL arithmetic using same-bucket ES|QL math
warning: Collapsed all series of `node_memory_SwapFree_bytes` into a single AVG line; the source selector has no series labels (no legend, by(), or dashboard reference), so per-series detail is dropped. Add a legend/by() or migrate with target access to recover per-series fidelity.
warning: Collapsed all series of `node_memory_SwapTotal_bytes` into a single AVG line; the source selector has no series labels (no legend, by(), or dashboard reference), so per-series detail is dropped. Add a legend/by() or migrate with target access to recover per-series fidelity.
warning: PromQL series labels were not retained; output is bucket-level and may collapse multiple source series
---
TS metrics-*
| WHERE @timestamp >= ?_tstart AND @timestamp <= ?_tend
| WHERE node_memory_SwapFree_bytes IS NOT NULL OR node_memory_SwapTotal_bytes IS NOT NULL
| STATS node_memory_SwapFree_bytes = AVG(node_memory_SwapFree_bytes), node_memory_SwapTotal_bytes = AVG(node_memory_SwapTotal_bytes) BY time_bucket = TBUCKET(5 minute)
| EVAL computed_value = (node_memory_SwapFree_bytes / CASE(node_memory_SwapTotal_bytes > 0, 1, NULL))
| KEEP time_bucket, computed_value
| SORT time_bucket ASC
